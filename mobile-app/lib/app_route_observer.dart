import 'package:flutter/material.dart';

/// Global route observer — profile page refreshes when pushed or re-shown.
final RouteObserver<PageRoute<dynamic>> appRouteObserver =
    RouteObserver<PageRoute<dynamic>>();
