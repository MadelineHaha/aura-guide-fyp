import 'package:flutter/material.dart';

/// Shared palette for patient and staff mobile apps.
abstract final class AppColors {
  static const background = Color(0xFF000000);
  static const card = Color(0xFF161616);
  static const accent = Color(0xFF63C3C4);
  static const subtext = Color(0xFFB0B0B0);
  static const text = Color(0xFFEFEFEF);
  static const border = Color(0xFF2E2E2E);
  static const highlightPanel = Color(0xFF142B2C);
}
